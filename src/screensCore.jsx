import React, { useMemo, useState } from 'react';
import {
  AlertTriangle, ArrowRight, CalendarDays, Camera, Check, ChevronDown, Eye, EyeOff,
  FileText, Filter, GraduationCap, HeartPulse, LockKeyhole, Mail, Search,
  ShieldCheck, Upload, X,
} from 'lucide-react';
import { Card, StatusBadge, CategoryBadge } from './ui';
import { formatCurrency, formatDate } from './format';

const fieldClass = 'w-full rounded-xl border border-slate-200 bg-white px-4 py-3.5 text-slate-950 outline-none transition placeholder:text-slate-400 focus:border-teal-600 focus:ring-4 focus:ring-teal-600/10 dark:border-slate-700 dark:bg-slate-900 dark:text-white';

function GoogleIcon() {
  return <svg viewBox="0 0 24 24" aria-hidden="true" className="h-5 w-5"><path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.77h3.57c2.08-1.92 3.27-4.74 3.27-8.1Z"/><path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.29-2.65l-3.57-2.77c-.98.66-2.24 1.06-3.72 1.06-2.87 0-5.3-1.94-6.17-4.54H2.15v2.84A11 11 0 0 0 12 23Z"/><path fill="#FBBC05" d="M5.83 14.1A6.6 6.6 0 0 1 5.48 12c0-.73.13-1.43.35-2.1V7.06H2.15A11 11 0 0 0 1 12c0 1.77.42 3.44 1.15 4.94l3.68-2.84Z"/><path fill="#EA4335" d="M12 5.36c1.62 0 3.06.56 4.2 1.64l3.17-3.17A10.6 10.6 0 0 0 12 1a11 11 0 0 0-9.85 6.06L5.83 9.9C6.7 7.3 9.13 5.36 12 5.36Z"/></svg>;
}

export function LoginScreen({ app }) {
  const [mode, setMode] = useState('login');
  const [name, setName] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [showPassword, setShowPassword] = useState(false);
  const submit = async (event) => {
    event.preventDefault();
    if (mode === 'forgot') { const sent = await app.resetPassword(email); if (sent) setMode('login'); return; }
    if (mode === 'signup') await app.createAccount({ name, email, password });
    else await app.emailLogin({ email, password });
  };

  return <main className="min-h-screen bg-[#f5f8f7] text-slate-950 dark:bg-slate-950 dark:text-white"><div className="grid min-h-screen lg:grid-cols-[1.05fr_.95fr]">
    <section className="relative hidden overflow-hidden bg-gradient-to-br from-teal-950 via-teal-800 to-[#075569] p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-40 -top-40 h-[520px] w-[520px] rounded-full bg-white/10 blur-3xl"/><div className="absolute -bottom-56 -left-40 h-[500px] w-[500px] rounded-full bg-emerald-400/15 blur-3xl"/><div className="relative flex items-center gap-4"><img src="/brand-mark.svg" alt="" className="h-16 w-16 rounded-2xl shadow-2xl"/><div><p className="text-2xl font-black">Recibos IR</p><p className="text-sm text-teal-100">Seus recibos organizados. Sua declaração tranquila.</p></div></div><div className="relative max-w-xl"><span className="inline-flex rounded-full bg-white/10 px-4 py-2 text-sm font-semibold text-teal-50 ring-1 ring-white/15">Organização inteligente para o Imposto de Renda</span><h1 className="mt-6 text-5xl font-black leading-[1.05] tracking-tight">Menos papelada.<br/>Mais tranquilidade.</h1><p className="mt-6 max-w-lg text-lg leading-8 text-teal-50/80">Fotografe seus comprovantes, revise a leitura da IA e mantenha tudo pronto para a declaração — sem a correria de última hora.</p><div className="mt-10 grid grid-cols-3 gap-4 text-sm"><div className="rounded-2xl bg-white/10 p-4 ring-1 ring-white/10"><ShieldCheck className="mb-3"/><b>Dados protegidos</b></div><div className="rounded-2xl bg-white/10 p-4 ring-1 ring-white/10"><FileText className="mb-3"/><b>Tudo organizado</b></div><div className="rounded-2xl bg-white/10 p-4 ring-1 ring-white/10"><Check className="mb-3"/><b>Revisão simples</b></div></div></div><p className="relative text-xs text-teal-100/70">Arquivos armazenados localmente no seu dispositivo.</p></section>
    <section className="flex items-center justify-center px-5 py-10 sm:px-10"><div className="w-full max-w-md"><div className="mb-8 flex items-center gap-3 lg:hidden"><img src="/brand-mark.svg" alt="" className="h-14 w-14"/><div><p className="text-xl font-black">Recibos IR</p><p className="text-xs text-teal-700 dark:text-teal-300">Sua declaração tranquila.</p></div></div><h2 className="text-3xl font-black tracking-tight">{mode === 'signup' ? 'Crie sua conta' : mode === 'forgot' ? 'Recupere seu acesso' : 'Que bom ter você aqui'}</h2><p className="mt-2 text-slate-500">{mode === 'signup' ? 'Comece agora e sincronize seus registros.' : mode === 'forgot' ? 'Enviaremos um link para redefinir sua senha.' : 'Entre para acessar seus recibos e comprovantes.'}</p>
      {mode !== 'forgot' && <button onClick={app.login} disabled={app.loginLoading} className="mt-7 flex w-full items-center justify-center gap-3 rounded-xl border border-slate-200 bg-white px-4 py-3.5 font-bold shadow-sm transition hover:bg-slate-50 disabled:opacity-60 dark:border-slate-700 dark:bg-slate-900 dark:hover:bg-slate-800"><GoogleIcon/>{app.loginLoading ? 'Concluindo…' : 'Continuar com Google'}</button>}{mode !== 'forgot' && <div className="my-6 flex items-center gap-3 text-xs font-semibold uppercase tracking-widest text-slate-400"><span className="h-px flex-1 bg-slate-200 dark:bg-slate-800"/>ou use seu e-mail<span className="h-px flex-1 bg-slate-200 dark:bg-slate-800"/></div>}
      <form onSubmit={submit} className="space-y-4">{mode === 'signup' && <label className="block text-sm font-bold">Nome<input required autoComplete="name" value={name} onChange={(event) => setName(event.target.value)} placeholder="Como podemos chamar você?" className={`${fieldClass} mt-1.5`}/></label>}<label className="block text-sm font-bold">E-mail<div className="relative mt-1.5"><Mail className="absolute left-4 top-3.5 text-slate-400" size={20}/><input required type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="voce@email.com" className={`${fieldClass} pl-12`}/></div></label>{mode !== 'forgot' && <label className="block text-sm font-bold">Senha<div className="relative mt-1.5"><LockKeyhole className="absolute left-4 top-3.5 text-slate-400" size={20}/><input required minLength={6} type={showPassword ? 'text' : 'password'} autoComplete={mode === 'signup' ? 'new-password' : 'current-password'} value={password} onChange={(event) => setPassword(event.target.value)} placeholder={mode === 'signup' ? 'Mínimo de 6 caracteres' : 'Sua senha'} className={`${fieldClass} px-12`}/><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-2.5 rounded-lg p-2 text-slate-400" aria-label={showPassword?'Ocultar senha':'Mostrar senha'}>{showPassword?<EyeOff size={19}/>:<Eye size={19}/>}</button></div></label>}{mode === 'login' && <button type="button" onClick={() => setMode('forgot')} className="text-sm font-bold text-teal-700 dark:text-teal-300">Esqueci minha senha</button>}{app.authError && <p role="alert" className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm font-medium text-red-700 dark:border-red-900/70 dark:bg-red-950/40 dark:text-red-300">{app.authError}</p>}{app.authNotice && <p role="status" className="rounded-xl border border-teal-200 bg-teal-50 px-4 py-3 text-sm font-medium text-teal-800 dark:border-teal-900/70 dark:bg-teal-950/40 dark:text-teal-200">{app.authNotice}</p>}<button disabled={app.loginLoading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-teal-700 px-4 py-4 font-bold text-white shadow-lg shadow-teal-800/15 transition hover:bg-teal-800 disabled:opacity-60">{app.loginLoading ? 'Aguarde…' : mode === 'signup' ? 'Criar minha conta' : mode === 'forgot' ? 'Enviar link de recuperação' : 'Entrar'}<ArrowRight size={18}/></button></form>
      <p className="mt-6 text-center text-sm text-slate-500">{mode === 'signup' ? 'Já tem uma conta?' : mode === 'forgot' ? 'Lembrou a senha?' : 'Ainda não tem uma conta?'} <button onClick={() => setMode(mode === 'signup' ? 'login' : mode === 'forgot' ? 'login' : 'signup')} className="font-black text-teal-700 dark:text-teal-300">{mode === 'signup' ? 'Entrar' : mode === 'forgot' ? 'Voltar ao login' : 'Criar conta'}</button></p><button onClick={app.guestLogin} className="mt-5 w-full text-center text-sm font-semibold text-slate-500 underline underline-offset-4">Continuar sem conta neste dispositivo</button><p className="mt-7 text-center text-[11px] leading-5 text-slate-400">Ao continuar, você concorda com os Termos de Uso e a Política de Privacidade.</p></div></section>
  </div></main>;
}

export function DashboardScreen({ app }) {
  const registered = app.yearExpenses.reduce((sum, item) => sum + Number(item.valor || 0), 0);
  const reviewItems = app.yearExpenses.filter((item) => (item.deductibilityStatus || 'review') === 'review');
  const confirmed = app.yearExpenses.length - reviewItems.length;
  const pct = app.yearExpenses.length ? Math.round((confirmed / app.yearExpenses.length) * 100) : 0;
  const health = app.yearExpenses.filter((item) => item.categoria === 'Saúde');
  const education = app.yearExpenses.filter((item) => item.categoria === 'Educação');
  const sum = (items) => items.reduce((total, item) => total + Number(item.valor || 0), 0);
  return <div className="space-y-6"><header className="flex items-center justify-between gap-4 pt-1"><div><p className="text-sm font-bold text-teal-700 dark:text-teal-300">Olá, {app.user.displayName?.split(' ')[0] || 'visitante'}! 👋</p><h1 className="mt-1 text-3xl font-black tracking-tight">Visão geral</h1><label className="mt-2 inline-flex items-center gap-2 text-sm text-slate-500">Ano-base<select value={app.filterYear} onChange={(event)=>app.setFilterYear(event.target.value)} className="bg-transparent font-bold text-slate-700 outline-none dark:text-slate-200">{app.years.map((year)=><option key={year}>{year}</option>)}</select><ChevronDown size={15}/></label></div><img src="/brand-mark.svg" alt="Recibos IR" className="h-12 w-12 lg:hidden"/></header>
    <div className="grid gap-5 xl:grid-cols-[1.35fr_.65fr]"><Card className="relative overflow-hidden border-0 bg-gradient-to-br from-teal-700 via-[#087e72] to-[#075569] p-6 text-white shadow-xl shadow-teal-900/15 sm:p-7"><div className="absolute -right-20 -top-20 h-60 w-60 rounded-full bg-white/10"/><div className="relative flex flex-col justify-between gap-6 sm:flex-row sm:items-start"><div><p className="text-sm font-semibold text-teal-50/85">Resumo do ano</p><p className="mt-1 text-4xl font-black tracking-tight">{formatCurrency(registered)}</p><p className="mt-2 text-sm text-teal-50/75">em {app.yearExpenses.length} recibo(s)</p></div><div className="rounded-2xl bg-white/10 p-3 ring-1 ring-white/15"><CalendarDays size={26}/></div></div><div className="relative mt-7"><div className="mb-2 flex justify-between text-xs font-semibold"><span>{confirmed} revisados</span><span>{pct}% concluído</span></div><div className="h-2 overflow-hidden rounded-full bg-black/20"><div className="h-full rounded-full bg-emerald-200" style={{width:`${pct}%`}}/></div></div></Card><button onClick={() => { app.resetScan(); app.navigate('scan'); }} className="group flex min-h-52 flex-col items-start justify-between rounded-[22px] border border-teal-200 bg-teal-50 p-6 text-left transition hover:-translate-y-0.5 hover:shadow-lg dark:border-teal-900 dark:bg-teal-950/30"><span className="rounded-2xl bg-teal-700 p-3 text-white"><Upload size={24}/></span><div><p className="text-xl font-black">Adicionar recibo</p><p className="mt-1 text-sm leading-6 text-slate-500">Fotografe ou envie um arquivo. A IA ajuda a preencher os dados.</p></div></button></div>
    {reviewItems.length > 0 && <section><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-black">Alertas pendentes</h2><span className="rounded-full bg-amber-100 px-2.5 py-1 text-xs font-black text-amber-800">{reviewItems.length}</span></div><button onClick={() => app.navigate('list')} className="flex w-full items-center gap-4 rounded-[22px] border border-amber-200 bg-amber-50 p-4 text-left dark:border-amber-900/60 dark:bg-amber-950/30"><span className="rounded-xl bg-amber-100 p-2.5 text-amber-700 dark:bg-amber-900/50 dark:text-amber-300"><AlertTriangle size={22}/></span><span className="min-w-0 flex-1"><b className="block">{reviewItems.length} recibo(s) aguardando revisão</b><small className="text-slate-500">Confira os campos extraídos pela IA</small></span><ArrowRight size={19}/></button></section>}
    <section><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-black">Por categoria</h2><button onClick={() => app.navigate('list')} className="text-sm font-bold text-teal-700 dark:text-teal-300">Ver todas</button></div><div className="grid gap-3 sm:grid-cols-2"><Card className="flex items-center gap-4 p-4"><span className="rounded-2xl bg-emerald-50 p-3 text-emerald-700 dark:bg-emerald-950/40 dark:text-emerald-300"><HeartPulse/></span><div><p className="text-sm font-bold">Saúde</p><p className="text-xl font-black">{formatCurrency(sum(health))}</p><small className="text-slate-500">{health.length} recibo(s)</small></div></Card><Card className="flex items-center gap-4 p-4"><span className="rounded-2xl bg-sky-50 p-3 text-sky-700 dark:bg-sky-950/40 dark:text-sky-300"><GraduationCap/></span><div><p className="text-sm font-bold">Educação</p><p className="text-xl font-black">{formatCurrency(sum(education))}</p><small className="text-slate-500">{education.length} recibo(s)</small></div></Card></div></section>
    <section><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-black">Recibos recentes</h2><button onClick={() => app.navigate('list')} className="text-sm font-bold text-teal-700 dark:text-teal-300">Ver todos</button></div><div className="grid gap-3 lg:grid-cols-2">{app.yearExpenses.slice(0,4).map((expense)=><Card key={expense.id} onClick={()=>app.openExpense(expense)} className="flex items-center justify-between gap-4 p-4"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-teal-50 text-teal-700 dark:bg-teal-950/40 dark:text-teal-300"><FileText size={20}/></span><div className="min-w-0 flex-1"><p className="truncate font-bold">{expense.razao_social}</p><p className="mt-0.5 text-xs text-slate-500">{formatDate(expense.data)} · {expense.categoria}</p></div><div className="text-right"><p className="font-black">{formatCurrency(expense.valor)}</p><StatusBadge status={expense.deductibilityStatus||'review'}/></div></Card>)}{!app.yearExpenses.length&&<Card className="p-8 text-center text-sm text-slate-500 lg:col-span-2">Seu painel está pronto. Adicione o primeiro recibo de {app.filterYear}.</Card>}</div></section>
  </div>;
}

export function ListScreen({ app, header }) {
  const [category, setCategory] = useState('Todos');
  const [status, setStatus] = useState('all');
  const categories = ['Todos','Saúde','Educação','Previdência','Outros'];
  const shown = useMemo(() => app.filteredExpenses.filter((item) => (category==='Todos'||item.categoria===category) && (status==='all'||(item.deductibilityStatus||'review')===status)), [app.filteredExpenses,category,status]);
  const total = shown.reduce((sum,item)=>sum+Number(item.valor||0),0);
  return <div>{header('Recibos')}<div className="mb-5 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-3.5 text-slate-400" size={19}/><input value={app.search} onChange={(event)=>app.setSearch(event.target.value)} placeholder="Buscar prestador, CPF/CNPJ ou descrição" className={`${fieldClass} pl-12`}/></div><select value={app.dependentFilter} onChange={(event)=>app.setDependentFilter(event.target.value)} className={`${fieldClass} sm:max-w-56`}><option value="all">Todos os beneficiários</option><option value="titular">Titular</option>{app.dependents.map((dependent)=><option key={dependent.id} value={dependent.id}>{dependent.name}</option>)}</select></div><div className="mb-4 flex gap-2 overflow-x-auto pb-1">{categories.map((item)=><button key={item} onClick={()=>setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm font-bold transition ${category===item?'bg-teal-700 text-white':'border border-slate-200 bg-white text-slate-500 dark:border-slate-800 dark:bg-slate-900'}`}>{item}</button>)}</div><div className="mb-5 flex items-center gap-2 border-b border-slate-200 dark:border-slate-800"><Filter size={16} className="text-slate-400"/>{[['all','Todos'],['potential','Revisados'],['review','Pendentes']].map(([value,label])=><button key={value} onClick={()=>setStatus(value)} className={`border-b-2 px-3 py-3 text-sm font-bold ${status===value?'border-teal-700 text-teal-700 dark:text-teal-300':'border-transparent text-slate-400'}`}>{label}</button>)}</div><div className="space-y-3">{shown.map((expense)=><Card key={expense.id} onClick={()=>app.openExpense(expense)} className="p-4"><div className="flex items-center gap-3"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-teal-50 text-teal-700 dark:bg-teal-950/40 dark:text-teal-300"><FileText size={20}/></span><div className="min-w-0 flex-1"><p className="truncate font-bold">{expense.razao_social}</p><p className="mt-0.5 text-xs text-slate-500">{formatDate(expense.data)} · {expense.dependentName||expense.dependente||'Titular'}</p></div><div className="text-right"><p className="font-black">{formatCurrency(expense.valor)}</p><StatusBadge status={expense.deductibilityStatus||'review'}/></div></div><div className="mt-3 flex gap-2 pl-14"><CategoryBadge category={expense.categoria}/>{expense.hasAttachment&&<span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-600 dark:bg-slate-800 dark:text-slate-300">comprovante</span>}</div></Card>)}{!shown.length&&<Card className="p-10 text-center"><Search className="mx-auto text-slate-300" size={34}/><p className="mt-3 font-bold">Nenhum recibo encontrado</p><p className="mt-1 text-sm text-slate-500">Tente ajustar a busca ou os filtros.</p></Card>}</div><Card className="mt-5 flex items-center justify-between p-4"><span className="text-sm font-bold text-slate-500">Total exibido · {shown.length} recibo(s)</span><b className="text-xl">{formatCurrency(total)}</b></Card></div>;
}

export function ScanScreen({ app }) {
  return <div className="fixed inset-0 z-50 overflow-y-auto bg-[#f5f8f7] p-5 text-slate-950 dark:bg-slate-950 dark:text-white"><div className="mx-auto flex min-h-full max-w-2xl flex-col justify-center py-8"><button onClick={app.back} className="absolute right-5 top-5 rounded-full border border-slate-200 bg-white p-2.5 shadow-sm dark:border-slate-700 dark:bg-slate-900"><X size={24}/></button><div className="text-center"><img src="/brand-mark.svg" alt="" className="mx-auto h-16 w-16"/><h2 className="mt-4 text-3xl font-black">Adicionar recibo</h2><p className="mt-2 text-slate-500">Escolha como deseja enviar o comprovante.</p></div><label className="mt-8 flex min-h-60 cursor-pointer flex-col items-center justify-center rounded-[28px] border-2 border-dashed border-teal-300 bg-white p-8 text-center transition hover:border-teal-600 hover:bg-teal-50/50 dark:border-teal-800 dark:bg-slate-900 dark:hover:bg-teal-950/20"><span className="rounded-2xl bg-teal-50 p-4 text-teal-700 dark:bg-teal-950/50 dark:text-teal-300"><Upload size={32}/></span><b className="mt-4 text-lg">Arraste o arquivo aqui</b><span className="mt-1 text-sm text-slate-500">ou toque para escolher uma imagem ou PDF</span><input type="file" accept="image/*,application/pdf" onChange={app.upload} className="hidden"/></label><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="flex cursor-pointer items-center gap-4 rounded-2xl border border-slate-200 bg-white p-4 font-bold shadow-sm dark:border-slate-800 dark:bg-slate-900"><span className="rounded-xl bg-[#e8f7f5] p-3 text-teal-700 dark:bg-teal-950/50 dark:text-teal-300"><Camera/></span><span>Tirar foto<small className="block font-normal text-slate-500">Usar a câmera</small></span><input type="file" accept="image/*" capture="environment" onChange={app.upload} className="hidden"/></label><label className="flex cursor-pointer items-center gap-4 rounded-2xl border border-slate-200 bg-white p-4 font-bold shadow-sm dark:border-slate-800 dark:bg-slate-900"><span className="rounded-xl bg-emerald-50 p-3 text-emerald-700 dark:bg-emerald-950/50 dark:text-emerald-300"><FileText/></span><span>Selecionar PDF<small className="block font-normal text-slate-500">Escolher arquivo</small></span><input type="file" accept="application/pdf" onChange={app.upload} className="hidden"/></label></div><div className="mt-5 flex items-center gap-3 rounded-2xl bg-teal-50 p-4 text-sm text-teal-900 dark:bg-teal-950/30 dark:text-teal-200"><ShieldCheck className="shrink-0" size={22}/><span>O arquivo é otimizado no aparelho e o original permanece armazenado somente neste dispositivo.</span></div></div></div>;
}
